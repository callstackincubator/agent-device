# Domain Docs

This repo uses a single-context domain-doc layout.

Expected locations:

- `CONTEXT.md` at the repo root for project domain language, when present.
- `docs/adr/` for architectural decision records, when present.

If a domain term becomes important during architecture or implementation work and no `CONTEXT.md` exists, create one lazily and keep it concise.
