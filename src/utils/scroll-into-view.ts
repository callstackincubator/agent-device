export const DEFAULT_SCROLL_INTO_VIEW_MAX_SCROLLS = 48;

// Android text scans re-dump the full UI hierarchy every attempt, so keep the
// default tighter than Apple/ref-backed paths.
export const DEFAULT_ANDROID_SCROLL_INTO_VIEW_MAX_SCROLLS = 8;
